/// Constants declaration module.

/// The key to access the `commits` in the `Context`.
pub const CONTEXT_COMMITS: &str = "commits";

/// The key to access the new `tag` of the repository in the `Context`.
pub const CONTEXT_NEW_TAG: &str = "new_tag";

/// The key to access the `user` in the `Context`.
pub const CONTEXT_USER: &str = "user";

/// The key to access the last `tag` of the repository in the `Context`.
pub const CONTEXT_LAST_TAG: &str = "last_tag";

/// The key to access the last `tag` of the repository in the `Context`.
pub const CONTEXT_RELEASE_ACTION: &str = "release_action";
